# 编译输出
build/
*.apk
*.aab

# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
# pubspec.lock 保留提交，CI 构建需要精确版本
# pubspec.lock

# Android 本地配置
android/local.properties
android/.gradle
android/app/debug.keystore

# IDE
.idea/
*.iml
.vscode/

# macOS
.DS_Store

# 依赖缓存（CI 会自动重装）
# 保留 pubspec.lock 以确保版本一致
